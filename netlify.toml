
[build]
  command = "npm run generate"
  functions = "functions"
  publish = "dist"
[dev]
  command = "npm run dev"
  targetPort = 8080
  port = 8888
[functions]
  directory = "api"
  node_bundler = "esbuild"
